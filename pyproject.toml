[project]
name = "hiremenow-backend"
version = "1.0.0"
description = "HireMeNow freelance marketplace backend API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
pythonpath = ["src"]
testpaths = ["tests"]
